{
  "name": "price-claw-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Manifest V3 Chrome extension for local BYOK product price extraction.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "build": "vite build",
    "test": "tsc -p tsconfig.test.json && node --test .tmp-tests/tests/*.test.js"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.9.3",
    "vite": "^6.3.5"
  }
}
